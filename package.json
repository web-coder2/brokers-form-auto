{
    "name": "brokers-form-auto",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "concurrently \"npm run server\" \"npm run client\"",
        "client": "cd client && npm install && npm run dev",
        "server": "cd server && npm install && nodemon server.js"
    },
    "author": "",
    "license": "ISC",
    "description": "",
    "dependencies": {
        "nodemon": "^3.1.10"
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}
